
@{
    Layout = "./_Layout";
}
<h1>hello</h1>
@* <div class="container mt-4">
    <div class="row">
        <div class="col-md-8"></div>
        <div class="col-md-4">
            <form asp-controller="Checkout" asp-action="Payment" method="post">
                <div class="card p-3 delivery-summary">
                    <h5 class="fw-bold">Sipariş Özeti</h5>
                    <p class="d-flex justify-content-between">
                        <span>Ürünün Toplamı</span>
                        <span class="fw-bold" @Model.CartProducts.Sum(x => x.TotalPrice) TL</span>
                    </p>
                    <p class="d-flex justify-content-between">
                        <span>Kargo Toplam</span>
                        <span class="fw-bold">44,99 TL</span>
                    </p>
                    <p class="d-flex justify-content-between text-success">
                        <span>250 TL ve Üzeri Kargo Bedava</span>
                        <span>-44,99 TL</span>
                    </p>
                    <hr>
                    <p class="d-flex justify-content-between">
                        <span class="fw-bold">Toplam</span>
                        <span class="fw-bold">@Model.CartProducts.Sum(x => x.TotalPrice) TL</span>
                    </p>
                    <a class="btn btn-warning w-100 mb-2 fw-bold" id="#bttnKaydet">
                        Kaydet ve Devam Et
                    </a>
                    <button type="submit" id="kaydetButton">Kaydet ve Devam Et</button>
                </div>
            </form>
        </div>
    </div>
</div> *@

